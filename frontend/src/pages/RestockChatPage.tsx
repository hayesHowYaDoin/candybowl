import ChatBase from '../components/ChatBase'

export default function RestockChatPage() {
  return (
    <ChatBase 
      mode="restock"
      title="Auto Restock"
      description="AI automatically restocks the bowl with profitable items"
    />
  )
}